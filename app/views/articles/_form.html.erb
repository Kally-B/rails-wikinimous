<%= simple_form_for @article.new_record? ? Article.new : @article do |f| %>
  <p><%= f.input :title %></p>

  <p><%= f.input :content %></p>

  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<p class="mt-2"><%= link_to "Back", articles_path %></p>
